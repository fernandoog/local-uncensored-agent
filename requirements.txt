# Requires Python 3.11 or 3.12 for CUDA prebuilt wheels (NOT 3.13).
# Install with: python install_deps.py
llama-cpp-python>=0.3.0
huggingface-hub>=0.29.0
pydantic>=2.10.0
rich>=13.9.0
pillow>=10.0.0
# Professional media (photoreal SD + neural TTS):
#   python install_media_deps.py
#   or: pip install -r requirements-media.txt
