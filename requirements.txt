# Requires Python 3.11 or 3.12 for CUDA prebuilt wheels (NOT 3.13).
# Install with: python install_deps.py
llama-cpp-python>=0.3.0
huggingface-hub>=0.29.0
pydantic>=2.10.0
rich>=13.9.0
pillow>=10.0.0
# Optional media upgrades (not required):
#   pyttsx3          -> offline TTS for generate_audio(mode=tts)
#   imageio[ffmpeg]  -> MP4 export for generate_video
#   Automatic1111/Forge local API via MEDIA_SD_API_URL for real diffusion images
